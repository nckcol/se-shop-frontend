import React, { Component } from 'react';
import styled from 'styled-components';

const Block = styled.div``;

class ProductPreview extends Component {
  render() {
    return <Block>Product</Block>;
  }
}

export default ProductPreview;
